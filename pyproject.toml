[project]
name = "crisisflow"
version = "0.1.0"
description = "CrisisFlow RL Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"